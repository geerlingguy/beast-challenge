---
services:

  leader:
    build:
      context: leader-app
    stop_signal: SIGINT
    restart: always
    volumes:
      - ./:/app
    ports:
      - '5000:5000'

  button:
    build:
      context: the-button
    stop_signal: SIGINT
    restart: always
    volumes:
      - ./:/app
    ports:
      - '5005:5000'
